function checkAge(name, age) {
  if (age >= 21)
  {
    
    return("Welcome, " + name + "!");
    
  }
  else
  {
    
    return("Go home, " + name + "!");
    
  }
}

console.log(checkAge("Billy", 21))
