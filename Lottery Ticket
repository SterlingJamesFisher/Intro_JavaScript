function greenTicket(a, b, c)
{
  
  if(a == b && b == c)
  {
    return(20);
  }
  else
  {
    if(a == b || b == c || a == c)
    {
      return(10);
    } else
    {
      return(0);
    }
  }
  
}

var output = greenTicket(2, 2, 2);
console.log(output);
